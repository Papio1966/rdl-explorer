import { beginApiRequest,completeApiRequest } from "../_runtime.ts";import { queryValue } from "../governance/_shared.ts";import { impactContext,handleApiError,type ApiRequest,type ApiResponse } from "./_shared.ts";export default async function handler(request:ApiRequest,response:ApiResponse){const context=beginApiRequest(request,response,"impact.compare");if(request.method!=="GET"){completeApiRequest(context,405);response.status(405).json({error:"Method not allowed."});return;}try{const {service}=impactContext(request);const analysis=await service.compare(Number(queryValue(request.query?.from)),Number(queryValue(request.query?.to)));completeApiRequest(context,200);response.status(200).json(analysis);}catch(error){handleApiError(response,error,context);}}
