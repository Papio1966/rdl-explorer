import { beginApiRequest,completeApiRequest } from "../_runtime.ts";import { queryValue } from "../governance/_shared.ts";import { impactContext,handleApiError,type ApiRequest,type ApiResponse } from "./_shared.ts";export default async function handler(request:ApiRequest,response:ApiResponse){const context=beginApiRequest(request,response,"impact.releases");if(request.method!=="GET"){completeApiRequest(context,405);response.status(405).json({error:"Method not allowed."});return;}try{const {service}=impactContext(request);const releases=await service.releases(Number(queryValue(request.query?.limit)||"100"));completeApiRequest(context,200,{count:releases.length});response.status(200).json({schemaVersion:"rdl-release-impact-catalogue/v1",releases});}catch(error){handleApiError(response,error,context);}}
