import { beginApiRequest,completeApiRequest } from "../_runtime.ts";import { impactContext,handleApiError,type ApiRequest,type ApiResponse } from "./_shared.ts";export default async function handler(request:ApiRequest,response:ApiResponse){const context=beginApiRequest(request,response,"impact.session");if(request.method!=="GET"){completeApiRequest(context,405);response.status(405).json({error:"Method not allowed."});return;}try{const {identity}=impactContext(request);completeApiRequest(context,200);response.status(200).json({authenticated:true,consumer:identity.reviewer,roles:identity.roles,authenticatedAt:identity.authenticatedAt,contract:"rdl-release-impact/v1"});}catch(error){handleApiError(response,error,context);}}
