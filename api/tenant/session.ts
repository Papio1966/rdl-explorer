import { beginApiRequest,completeApiRequest } from "../_runtime.ts";
import { handleTenantError,tenantContext,type ApiRequest,type ApiResponse } from "./_shared.ts";
export default async function handler(request:ApiRequest,response:ApiResponse){const context=beginApiRequest(request,response,"tenant.session");if(request.method!=="GET"){completeApiRequest(context,405);response.status(405).json({error:"Method not allowed."});return}try{const{identity,globalRoles,scope,service}=await tenantContext(request);const data=await service.tenantSession(identity,scope.organizationKey,globalRoles);completeApiRequest(context,200,{organizationKey:scope.organizationKey,subject:identity.subject});response.status(200).json(data)}catch(error){handleTenantError(response,error)}}
