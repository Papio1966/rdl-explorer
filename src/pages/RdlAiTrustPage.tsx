import { AlertTriangle, BarChart3, CheckCircle2, FlaskConical, MessageSquareWarning, ShieldCheck } from "lucide-react";
import { useEffect,useState } from "react";
import { Link } from "react-router-dom";
import { AI_TRUST_DEMO } from "../rdl/aiTrustDemo";
import { loadAiTrustSession,loadAiTrustSummary,type AiTrustSession,type AiTrustSummary } from "../rdl/aiTrustService";
import "./RdlAiTrustPage.css";

export function RdlAiTrustPage(){const[session,setSession]=useState<AiTrustSession|null>(null);const[live,setLive]=useState<AiTrustSummary|null>(null);useEffect(()=>{let active=true;Promise.all([loadAiTrustSession(),loadAiTrustSummary()]).then(([s,d])=>{if(!active)return;setSession(s);setLive(s&&d?d:null)});return()=>{active=false}},[]);const data=live??AI_TRUST_DEMO;const demo=!live;const m=data.metrics;return <div className="ai-trust-page"><header><div className="ai-trust-eyebrow"><FlaskConical size={16}/> RDL-026 · AI Evaluation, Feedback & Trust Controls</div><h1>AI trust & evaluation</h1><p>Measure groundedness, evidence coverage, unsupported claims and user feedback before expanding AI authority. AI remains advisory and model promotion remains a human-controlled release decision.</p></header><section className={`ai-trust-mode ${demo?"demo":"live"}`}><ShieldCheck size={18}/><div><strong>{demo?"Read-only trust demonstration":"Trusted AI quality telemetry"}</strong><span>{demo?"No live enterprise AI evaluation data is exposed without a trusted analyst session.":`Authenticated as ${session?.reviewer}`}</span></div></section><section aria-labelledby="trust-metrics-heading"><h2 id="trust-metrics-heading">Trust metrics</h2><div className="ai-trust-grid"><article><CheckCircle2/><strong>{pct(m.helpful_rate)} helpful</strong><span>{m.feedback_count} feedback records</span></article><article><BarChart3/><strong>{pct(m.avg_groundedness_score)} grounded</strong><span>{m.evaluation_count} evaluation results</span></article><article><ShieldCheck/><strong>{pct(m.avg_evidence_coverage_score)} evidence coverage</strong><span>{m.evidence_concern_count} evidence concerns</span></article><article><AlertTriangle/><strong>{m.unsupported_claim_count} unsupported claims</strong><span>{m.fail_count} failed · {m.review_count} review</span></article></div></section><section aria-labelledby="recent-runs-heading"><h2 id="recent-runs-heading">Recent advisory runs & feedback</h2><div className="ai-trust-table" tabIndex={0} aria-label="Recent AI advisory runs and feedback"><table><thead><tr><th>Run</th><th>Intent</th><th>Model / prompt</th><th>Evidence</th><th>Feedback</th></tr></thead><tbody>{data.recentRuns.length?data.recentRuns.map(r=><tr key={r.advisory_key}><th scope="row"><code>{r.advisory_key}</code></th><td>{human(r.intent)}</td><td>{r.model_key}<br/><small>{r.prompt_version}</small></td><td>{r.evidence_item_count}</td><td>{r.helpful_count} helpful · {r.incorrect_count} incorrect · {r.incomplete_count} incomplete</td></tr>):<tr><td colSpan={5}>No advisory runs recorded.</td></tr>}</tbody></table></div></section><section aria-labelledby="evaluation-heading"><h2 id="evaluation-heading">Evaluation regression</h2><p>Versioned evaluation cases make prompt/model comparisons repeatable. Results are immutable; a failing result cannot be overwritten or silently promoted.</p><div className="ai-trust-table" tabIndex={0} aria-label="AI evaluation regression results"><table><thead><tr><th>Case</th><th>Model / prompt</th><th>Groundedness</th><th>Coverage</th><th>Unsupported</th><th>Verdict</th></tr></thead><tbody>{data.recentEvaluations.length?data.recentEvaluations.map(e=><tr key={e.result_key}><th scope="row">{e.case_key} v{e.case_version}</th><td>{e.model_key}<br/><small>{e.prompt_version}</small></td><td>{pct(e.groundedness_score)}</td><td>{pct(e.evidence_coverage_score)}</td><td>{e.unsupported_claim_count}</td><td>{human(e.verdict)}</td></tr>):<tr><td colSpan={6}>No evaluation results recorded.</td></tr>}</tbody></table></div></section><section className="ai-trust-principles" aria-labelledby="trust-principles-heading"><h2 id="trust-principles-heading">Trust-control principles</h2><div><MessageSquareWarning/><span><strong>Feedback is append-only.</strong> Helpful, incorrect and incomplete classifications remain part of the audit trail.</span></div><div><FlaskConical/><span><strong>Evaluation is reproducible.</strong> Cases, prompt versions and model versions are recorded explicitly.</span></div><div><ShieldCheck/><span><strong>No automatic promotion.</strong> Quality metrics never auto-promote a model, prompt, mapping or governance decision.</span></div><div><AlertTriangle/><span><strong>Unsupported claims are visible.</strong> Uncertain or insufficient evidence remains review-required rather than being normalized as truth.</span></div></section><div className="ai-trust-actions"><Link to="/ai-intelligence">AI Standards Intelligence</Link><Link to="/control-tower">Standards Control Tower</Link></div></div>}
function pct(value:number){return `${Math.round(Number(value||0)*100)}%`}
function human(value:string){return String(value).replaceAll("_"," ")}
