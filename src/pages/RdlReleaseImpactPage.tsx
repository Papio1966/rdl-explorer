import { AlertTriangle,FileDiff,GitCompareArrows,ShieldCheck } from "lucide-react";import { useEffect,useState } from "react";import { RELEASE_IMPACT_DEMO } from "../rdl/releaseImpactDemo";import { loadReleaseImpactSession,type ReleaseImpactAnalysis,type ReleaseImpactSession } from "../rdl/releaseImpactService";
export function RdlReleaseImpactPage(){const [session,setSession]=useState<ReleaseImpactSession|null>(null);const [analysis]=useState<ReleaseImpactAnalysis>(RELEASE_IMPACT_DEMO);const [filter,setFilter]=useState<"all"|"breaking"|"added"|"removed"|"modified">("all");useEffect(()=>{let a=true;loadReleaseImpactSession().then(s=>a&&setSession(s));return()=>{a=false}},[]);const items=analysis.items.filter(i=>filter==="all"||(filter==="breaking"?i.breaking:i.kind===filter));return <div className="page rdl-hierarchy-page"><header className="page-header"><div><span className="eyebrow">RDL-021 · Release intelligence</span><h1>Release change intelligence & impact analysis</h1><p>Compare two exact published releases, classify semantic deltas, identify potentially breaking changes and assess pinned consumer impact before any migration decision.</p></div></header><section className="rdl-hierarchy-callout" aria-labelledby="impact-boundary-heading"><ShieldCheck size={24}/><div><h2 id="impact-boundary-heading">Advisory impact boundary</h2><p>{session?`Authenticated package consumer: ${session.consumer}`:"Read-only impact demonstration"}. Analysis informs migration decisions; it never auto-migrates or auto-activates a project or consumer.</p></div></section><section className="rdl-hierarchy-section" aria-labelledby="comparison-heading"><h2 id="comparison-heading">Published release comparison</h2><div className="rdl-publication-summary-grid"><article><GitCompareArrows/><strong>{analysis.fromRelease.version} → {analysis.toRelease.version}</strong><span>{analysis.fromRelease.key}</span></article><article><AlertTriangle/><strong>{analysis.summary.breaking} potentially breaking</strong><span>{analysis.summary.nonBreaking} non-breaking changes</span></article><article><FileDiff/><strong>{analysis.consumerImpact.activated} activated consumers</strong><span>{analysis.consumerImpact.staged} staged · {analysis.consumerImpact.discovered} discovered</span></article></div></section><section className="rdl-hierarchy-section" aria-labelledby="delta-heading"><h2 id="delta-heading">Semantic release delta</h2><div className="rdl-publication-actions" role="group" aria-label="Release delta filters">{(["all","breaking","added","modified","removed"] as const).map(v=><button type="button" key={v} aria-pressed={filter===v} onClick={()=>setFilter(v)}>{v[0].toUpperCase()+v.slice(1)}</button>)}</div><div className="rdl-table-scroll" tabIndex={0} aria-label="Release delta table"><table><thead><tr><th>Entity</th><th>Change</th><th>Compatibility</th><th>From</th><th>To</th><th>Provenance</th></tr></thead><tbody>{items.map(i=><tr key={i.key}><th scope="row">{i.entityType}<br/><code>{i.nativeIdentifier}</code></th><td>{i.kind}</td><td>{i.breaking?"Potentially breaking":"Non-breaking"}</td><td>{i.fromName??"—"}</td><td>{i.toName??"—"}</td><td>{i.sourceLayer??"published package"}</td></tr>)}</tbody></table></div></section><section className="rdl-hierarchy-section" aria-labelledby="notes-heading"><h2 id="notes-heading">Governed release notes</h2><h3>{analysis.releaseNotes.title}</h3><ul>{analysis.releaseNotes.highlights.map(h=><li key={h}>{h}</li>)}</ul><p className="rdl-intelligence-copy">Analysis fingerprint: <code>{analysis.analysisSha256}</code></p></section><section className="rdl-hierarchy-section" aria-labelledby="decision-heading"><h2 id="decision-heading">Migration decision support</h2><p className="rdl-intelligence-copy">Pinned projects and consumers remain on their current release. Breaking-change classification is advisory and must be reviewed against engineering context before a new release is staged and activated.</p></section></div>}
